<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Login</title>

  <link href="https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-teal-100">

  <div class="flex items-center justify-center h-screen">

    <div class="bg-white p-16 rounded-lg shadow-2xl max-w-lg">

      <h1 class="text-3xl font-bold text-purple-600 text-center mb-4">Login</h1>

      <form action="/login" method="post">
      
        <div>
          <label for="username" class="text-gray-700 font-bold">Username</label>
          <input type="text" name="username" id="username" required class="w-full border border-purple-200 rounded-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline">
        </div>
        
        <div class="mt-4">
          <label for="password" class="text-gray-700 font-bold">Password</label>
          <input type="password" name="password" id="password" required class="w-full border border-purple-200 rounded-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline">
        </div>

        <button type="submit" class="bg-purple-500 hover:bg-purple-600 text-white font-bold py-2 px-4 rounded-full mt-6">
          Login
        </button>

        <% if (message) { %>
          <p class="text-red-500 mt-4"><%= message %></p>
        <% } %>

      </form>

    </div>

  </div>

</body>

</html>
